enum Position { left, right, start, center }
enum ButtonType { primary, secondary, disable }
enum Dimension { width, height }
enum InputType { field, dropdown, textArea }
enum ViewState { idle, busy }

/// [Status] : Representing default, success
/// warning and error state
enum Status { normal, success, warning, error }
enum DataType { string, int, double, bool, stringList }
